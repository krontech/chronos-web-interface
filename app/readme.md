To get started, in the project folder, run:

	sudo apt install nodejs npm
	npm install

Then, to lint your files, run:

	npx eslint index.js

You'll probably want to use an editor with support, however.